# Plan — 16/05/2026

## Objetivos Prioritarios

### 🔴 1. Arreglar Bug de Persistencia (BUG-1)
**Problema**: Solo 2 de 8 emails se guardan en BD. El resto falla con `db_save=FALLO`.

**Investigación pendiente**:
- [ ] Verificar el error exacto en el detalle de ActionResult (el script de captura no imprimió el detail)
- [ ] Comprobar si `message_id` se repite entre emails enviados en el mismo batch SMTP
- [ ] Comprobar si `_get_or_create_account_id` causa conflictos al crear Account duplicada
- [ ] Verificar que el rollback de `_save_to_db` no interfiere con emails posteriores
- [ ] Probar con `async with session.begin_nested()` para savepoints

**Posible solución**: Hacer que `_save_email` use `message_id` auto-generado si el original está vacío, o debuggear la razón exacta del fallo.

### 🟡 2. Ajustar Prompt del LLM Classifier (BUG-3)
**Problema**: El LLM (qwen2.5:7b) vota `nulo` con 0% de confianza para todos los emails.

**Tareas**:
- [ ] Revisar el prompt en `llm_agent.py` — probablemente necesita ejemplos (few-shot)
- [ ] Añadir ejemplos concretos de cada categoría en el prompt
- [ ] Probar con diferentes modelos (phi4-mini, gemma3)
- [ ] Verificar temperatura y otros parámetros

### 🟡 3. Fine-Tuning de BERT (BUG-2)
**Problema**: BERT clasifica todo como `nulo` porque el modelo descargado no está fine-tuneado.

**Tareas**:
- [ ] Generar dataset de entrenamiento (~500 emails etiquetados por categoría)
- [ ] Fine-tune DistilBERT con `train_bert_classifier.py`
- [ ] Verificar precisión en test set

### 🟢 4. Prueba End-to-End Completa
**Tareas**:
- [ ] Arreglar BUG-1 (DB save) y BUG-3 (LLM prompt)
- [ ] Limpiar inbox (forwarded copies)
- [ ] Enviar 5 nuevos emails de prueba
- [ ] Sincronizar y verificar:
  - [ ] Los 5 emails guardados en BD
  - [ ] Clasificación correcta (votos visibles en dashboard)
  - [ ] Enrutamiento a departamentos correctos
  - [ ] Reenvío SMTP a alias Gmail (+contabilidad, etc.)
  - [ ] Dashboard actualizado con badges
- [ ] Verificar emails reenviados en Gmail (+alias)

### 🟢 5. Registro de Errores y Logging
**Tareas**:
- [ ] Añadir logging estructurado al Orchestrator
- [ ] Registrar tiempos de cada paso del pipeline
- [ ] Guardar trace de errores en BD para debugging

### 🟢 6. Limpieza de Código
**Tareas**:
- [ ] Eliminar scripts de prueba temporales (`check_*.py`, `debug_*.py`, `test_db_*.py`)
- [ ] Mover `send_test_emails.py` y `test_orchestrator.py` a `backend/tests/`
- [ ] Estandarizar nombres de scripts de utilidad

### ⚪ 7. Documentación
- [ ] Día 1 finalizar: `docs/DOCUMENTACION_COMPLETA_PROYECTO.md` con el nuevo multi-agente
- [ ] README.md actualizado con arquitectura multi-agente

## Orden de Trabajo Recomendado

1. **BUG-1 Persistencia** → Desbloquea toda la validación end-to-end
2. **BUG-3 Prompt LLM** → Desbloquea clasificador inteligente (ajuste rápido)
3. **Prueba completa** → Validar que todo funciona
4. **BUG-2 BERT** → Solo si sobra tiempo (fine-tuning lleva horas)
5. **Limpieza y docs** → Al final

## Comandos Útiles

```bash
# Iniciar backend
cd backend && uvicorn src.api.main:app --reload

# Iniciar frontend
cd frontend && npm run dev

# Enviar emails de prueba
cd backend && python send_test_emails.py

# Sincronizar (vía API)
curl -X POST http://localhost:8001/api/v1/emails/sync \
  -H "Authorization: Bearer $(curl -s -X POST http://localhost:8001/api/v1/auth/login \
    -H 'Content-Type: application/json' \
    -d '{"username":"admin","password":"<ADMIN_PASSWORD_DEMO>"}' | python -c "import sys,json;print(json.load(sys.stdin)['access_token'])")"
```

## Estado del Sistema
- Backend: `http://localhost:8001` ✅
- Frontend: `http://localhost:5175` ✅
- Ollama: OK (6 modelos, qwen2.5:7b principal) ✅
- IMAP Gmail: Conectado (App Password) ✅
- SMTP Gmail: Funcionando ✅
- **BD: 2/8 emails guardados** ❌
- **BERT: no fine-tuneado** ❌
- **LLM prompt: necesita ajuste** ❌
