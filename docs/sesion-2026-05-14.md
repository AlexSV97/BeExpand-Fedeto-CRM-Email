# Sesión 2026-05-14

## Resumen

Completada Fase 2 del clasificador híbrido de 3 niveles (RuleEngine → BERT → Ollama).
Rediseño completo del Dashboard y Layout del frontend.

## Logros

### Pipeline híbrido (3 niveles)
- ✅ **RuleEngine ponderado**: Keywords con peso individual (1-4) + desempate por prioridad de categoría (proveedor > lead > cliente). Resuelve 9/9 casos en test sintético.
- ✅ **DistilBERT integrado**: Fine-tuneado con datos reales + aumentados + sintéticos (316 muestras). Calibrado: 88-95% en aciertos, 47-49% en dudosos. Threshold: 0.80.
- ✅ **Ollama/llama3.2**: Tercera capa para casos ambiguos. Prompt estructurado con JSON response.
- ✅ **Fiabilidad general**: 92% (12/13 escenarios sintéticos).
- ✅ **IMAP sync**: 10 correos reales clasificados sin errores en prueba end-to-end.

### Frontend
- ✅ **Dashboard**: 4 KPI cards con SVG icons, feed de últimos 10 emails clasificados con badges por método, donut de distribución, sincronización IMAP desde el UI.
- ✅ **Layout**: Sidebar con SVG icons (dashboard, contactos, oportunidades, logout), header con breadcrumb y avatar de usuario.

### Backend
- ✅ **Dashboard API**: Nuevas queries `_recent_emails()` y `_classification_by_method()` con subquery para evitar sobre-contar.
- ✅ **Configuración Ollama**: url, model, timeout en Settings.
- ✅ **Hybrid training script**: `train_bert_hybrid.py` — extrae datos reales de BD, aumenta con sinónimos/shuffle/dropout, combina con sintéticos.

## Estado de los servicios

- **Backend**: http://localhost:8001 — API REST (FastAPI + uvicorn)
- **Frontend**: http://localhost:5173 — Dashboard (Vite 8 + React 19)
- **Login**: admin / admin123

## Pendiente / Ideas

- [ ] Re-entrenar BERT con más datos reales (a medida que classification_history crezca con IMAP syncs diarios)
- [ ] Migrar de SQLite a PostgreSQL para producción
- [ ] Integración VTiger para sincronizar contactos y oportunidades
- [ ] Agregar tests unitarios para el pipeline híbrido (los 13 escenarios como test formal)
- [ ] Dashboard: agregar filtros por fecha, exportar reportes
- [ ] El servidor debe arrancarse con IMAP_EMAIL y IMAP_PASSWORD en environment variables

## Comandos útiles

```bash
# Arrancar backend
cd backend
$env:IMAP_EMAIL="beexpandcrmpoc@gmail.com"
$env:IMAP_PASSWORD="wdjqgpkkurdoxiqg"
uvicorn src.api.main:app --host 0.0.0.0 --port 8001 --reload

# Arrancar frontend
cd frontend
npx vite --port 5173

# Re-entrenar BERT con datos reales
cd backend
python scripts/train_bert_hybrid.py

# Test del clasificador
cd backend
python scripts/test_classifier.py --verbose
python scripts/test_classifier.py --inject-db  # Poblar BD con datos de prueba
```

## Hardware / Setup

- OS: Windows 11
- Python 3.12
- Node: 22.x
- Vite 8 con Rolldown (bundler)
- BERT: DistilBERT multilingual cased (CPU, ~50ms/inferencia)
- Ollama: llama3.2:3b local
- DB: SQLite (aiosqlite)
- GPU: No disponible — todo en CPU
