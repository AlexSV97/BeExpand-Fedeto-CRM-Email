# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
aiosqlite>=0.20.0
asyncpg>=0.30.0

# Validation & config
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# HTTP client (VTiger API)
httpx>=0.28.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0,<5.0.0
python-multipart>=0.0.18

# Task queue
celery>=5.4.0
redis>=5.2.0

# ML / NLP
# transformers para tokenizer, onnxruntime para inferencia (sin torch)
# El modelo fine-tuneado se descarga de HuggingFace Hub como ONNX INT8
transformers>=4.30.0
onnxruntime>=1.15.0  # Reemplaza a torch: ~130MB RAM, sin GPU
numpy>=1.24.0  # Para softmax manual en inferencia ONNX

# HuggingFace Hub (descarga modelo ONNX)
huggingface_hub>=0.20.0

# Development
pytest>=8.3.0
pytest-asyncio>=0.24.0
